# Deferred Work

> Auto-generated by BMad code-review workflow. Items deferred during code review due to being pre-existing, out of scope, or enhancement-level.

---

## Deferred from: code review of 1-2-user-registration-first-admin-init (2026-06-09)

- CLI `--password` 参数暴露于进程列表 — 保持 `--password` 参数设计（AC1 一致性优先）
- 密码强度策略过于宽松 — 代码精确匹配 AC3 要求，加强是产品决策
- Redis 故障导致登录全面失败 — auth_service 属于 Story 1-3 范围
- refresh_token 绕过账户锁定 — auth_service 属于 Story 1-3 范围
- 健康检查不验证后端连接 — Story 1-1 遗留
- CORS 空值处理 — Story 1-1 遗留
- bcrypt 成本因子不可配置 — enhancement
- 导入放入函数体内 — 循环依赖设计缺陷
- 缺少 jti/nbf JWT 声明 — enhancement
- Redis b"1" vs "1" 测试不一致 — Story 1-3 范围
- 缺少畸形 Authorization 头测试 — 测试覆盖 enhancement

## Deferred from: Agent API Frontend Integration review (2026-06-09)

- `AgentUpdateInput` 直接等同于 `AgentCreateInput`，PUT 更新时可选字段可能导致后端静默清空未传字段 — 待确认后端 PUT 语义（全量覆盖 vs partial update）后再决定是否需要调整类型定义

## Deferred from: code review of 2-4-agent-edit-copy-and-version-management (2026-06-10)

- 状态机转换无后端校验 — publish/archive 不检查来源状态，任意状态可互转 [agent_service.py:360-438] — 需独立 Story 统一设计状态机
- 并发竞态：TOCTOU 名称检查无事务保护 — create/update/duplicate 先查后写 [agent_service.py:52-58,202-210,469-480] — 依赖 MongoDB 唯一索引兜底
- 版本自增缺乏原子性，并发可能丢版本 — 读-改-写应改用 $inc [agent_service.py 多处]
- llm_config 用裸 dict 无嵌套校验 — AgentCreate/Update 不复用 ModelConfigUpdate schema
- 默认 llm_config 字典硬编码 6+ 次 — 应提取为共享常量
- _doc_to_response 缺少 KeyError 防护 — 脏数据文档导致 500
- create_agent 返回本地 doc 而非查询数据库结果 — 与其他方法不一致
- invoke/stream 端点在 API 层内联业务逻辑 — 绕过 Service 层（Epic 3 范围）
- invoke 端点缺少超时控制 — docstring 承诺 504 但无实现（Epic 3）
- SSE stream 错误不通过事件传递 + JSON 拼接注入风险 — （Epic 3 范围）
- invoke 的 output 字段将 messages 列表 str() 化 — 非 JSON（Epic 3）
- delete_agent 允许删除已发布 Agent 仅打印 warning — 待 Task 模型就绪后处理
- create_agent 异常处理将所有非 DuplicateKey 错误降级为 422
- Drawer loading 状态不会随 mutation 更新 — React ref 机制限制
- publish/archive mutation 在列表页和表单页重复定义 — 应提取为共享 hook
- publishMutation.isPending 全局禁用所有卡片按钮 — 未按 agentId 隔离
- 前端列表缺少分页控件 — page_size=50 硬编码
- stream 方法绕过 apiClient 丢失 token 刷新 + 缺少取消支持 — SSE 架构限制
- agents-page 编辑按钮 navigate 但 drawer edit 模式未使用 — AC1 架构选择
- 表单未阻止用户离开时丢失未保存修改 — UX 增强
- update_agent 允许通过 status 字段任意切换状态 — 与状态机校验同属独立 Story
- tool_ids/workflow_ids/knowledge_base_ids 不校验引用存在性 — 待 API 就绪
- 测试 fixture 修改全局 app.dependency_overrides — pre-existing
- 缺少 invoke/stream 端点测试 — Epic 3 范围
- 契约测试 kwargs 集合未包含 status 参数 — 与 status 漏洞相关
- test_create_agent_201 断言验证 mock 返回值而非输入传递 — mock 固有限制
- availableModels 查询硬编码 page_size:100 — MVP 可接受
- formatTime 无缓存且对非法时间产生 NaN — UX 优化
- isAgentError 类型守卫过于宽泛 — 低影响
- versionAgent 命名暗示版本历史但仅展示当前 — AC6 MVP 已知限制
- 延迟导入风格不一致 — pre-existing
- 卡片 onClick div 缺少键盘可访问性 — UX 增强

## Deferred from: code review of skill-injection-into-agent (2026-06-10)

- `tool_ids` 无去重 — `_resolve_tools` 应清理重复项再注入
- `tool_ids` 无上限 — Agent model 允许任意数量 ID 但未验证，应设置上限（如 50）
- DRAFT/INACTIVE 状态 Skill 未过滤 — 非活跃 Skill 不应被声明或加载，需在 `get_tools_by_ids` 或 `build_skill_declaration` 中排除

## Deferred from: code review of tool-categorized-config (2026-06-10)

- 编辑 Agent 配置的 Drawer 关闭时缺少未保存更改确认 — 属于 UX 增强的 common pattern，非本 Story 范围

## Deferred from: one-shot MCP 操作按钮精简 + URL 视觉修复 (2026-06-10)

- MCP 页面 URL `<code>` 元素缺少 `title` 属性 — 长 URL 被 `truncate` 截断后用户无法查看完整地址，可在后续改版中添加 `title={conn.url}`
