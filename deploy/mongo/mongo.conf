# MongoDB configuration (standalone, no replica set for MVP)
storage:
  dbPath: /data/db

net:
  bindIp: 0.0.0.0
  port: 27017

# Disable authentication for local dev; enable in production
# security:
#   authorization: enabled

log:
  verbosity: 1
