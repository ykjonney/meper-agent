"""Global FastAPI dependency injection helpers."""


# Placeholder dependency types for later stories
# Story 1.3 will add get_current_user, get_current_active_user
# Story 1.4 will add require_role


# Reusable type alias for fast dependency annotation
# Example: user_id: CurrentUserId = Depends(get_current_user_id)
# CurrentUserId = Annotated[str, Depends(get_current_user_id)]
