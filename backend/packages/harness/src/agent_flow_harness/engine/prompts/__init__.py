"""REACT prompt templates (v0.1-1 placeholder).

The 6-slot system prompt templates (``REACT`` system prompt, slot schema)
are populated in Story v0.1-6 (Slot renderer). This module exists so the
public API surface and import paths are stable from v0.1-1.
"""

from __future__ import annotations

__all__: list[str] = []
