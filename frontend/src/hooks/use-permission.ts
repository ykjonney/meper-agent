/**
 * Permission check hook placeholder.
 * Real implementation in Story 1.4 (RBAC).
 */
export function usePermission(permission: string): boolean {
  void permission // placeholder — real check in Story 1.4
  return false
}
