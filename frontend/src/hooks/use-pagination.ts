/**
 * Pagination state hook placeholder.
 * Real implementation in later stories.
 */
export function usePagination(defaultPageSize = 20) {
  return { page: 1, pageSize: defaultPageSize, total: 0 }
}
