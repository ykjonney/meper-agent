/**
 * Role-based route filter placeholder.
 * Real implementation in Story 1.4/1.7.
 */
export function filterRoutesByRole<T>(routes: T[], role: string): T[] {
  void role // placeholder — real filtering in Story 1.4/1.7
  return routes
}
