/**
 * WebSocket client placeholder.
 * Real implementation in Story 7.2 (streaming).
 */
export class WsClient {
  connect(url: string): void {
    void url // placeholder
  }

  disconnect(): void {
    // placeholder
  }
}

export const wsClient = new WsClient()
